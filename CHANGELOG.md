# 变更记录

## 0.1.1 — 2026-09-20

**修复**

- **误报修复（重要）**：`return` 不带分号时（ASI 写法），表达式提取会一路吞到函数体结尾，
  把后面语句里的 `.join()` / `String()` 也算进这个 return —— 于是"所有 return 都是数组"
  的**合规函数被判成"返回字符串"**。
  真实案例：`@tianbuyu-wwx/dsh-formatforge` 的 `render`（两条 return 都是数组）。
  改为按括号/引号深度扫描的 `scanReturnEnd()`。
- CI：修复 workflow 的 YAML 语法错误（内联脚本里的「冒号+空格」让整份文件失效，
  表现为 workflow 直接起不来、jobs 数为 0）。同时把"零依赖""产物与源码同步"
  等约束搬进测试、并加源码指纹，避免同类问题再现。

**文档订正**

- 修正一条**错误论断**：早前写的"插件写自定义事件名 = 会话打不开，**且作者无法自救**"
  不成立。`KNOWN_SESSION_EVENT_TYPES` 是**导出的可变 `Set`**，插件可在运行时 `.add()`
  注册自己的类型（生态实证：`@flowingspring/dsh-voco`）。
  准确表述是：**默认会打不开；但插件可以靠注册自救，前提是必须同时完成注册。**
- 运行时告警与 `checkEventType` 的判定理由改为条件式表述
  （"若该类型从未被注册……请确认你是否注册过"），不再断言一定会坏。
- 新增英文 README（`README.en.md`）。
- 补充：接口改为"会话头部一个圆点 + 「体检」按钮"（此前版本描述的是已删除的体检 Tab）。

**新增**

- 6 项包元信息测试（零依赖 / publishConfig / `files` 覆盖声明文件 / workflow 内联脚本禁含冒号+空格 等）。
- 5 项针对上述误报的回归测试。
- CI workflow：最小权限（`contents: read`）、**不跑 `npm install`**（零依赖，杜绝装依赖的风险面）。

**测试**：142 → 180 项。

---

## 0.1.0 — 2026-09-19

首个可用版本。

- 检查 `output.render()` 是否返回 `ContentBlock[]`（静态 + 实测探针，默认开启）
- 检查会话事件类型是否在内核词表内（44 类，取自 `@deepseek-ai/dsh-session@0.1.0-rc.6`）
- 检查打包白名单（声明要用的文件有没有被 `files` 漏在包外）
- GUI：会话头部颜色圆点 + 「体检」按钮；设置页含自愿打赏栏
- **零 token 成本**（不注册模型工具、不注入提示词）
- 只告警、不拦截；宁 `unknown`，不 `violation`
